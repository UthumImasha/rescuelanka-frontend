mutation UpdateRefugeeCamp($inputData: RefugeeCampGQLInput!) {
    updateRefugeeCamp(inputData: $inputData) {
        id
        name
        latitude
        longitude
        address
        capacity
        currentOccupancy
        servicesOffered
        status
        createdAt
        lastUpdatedAt
    }
}