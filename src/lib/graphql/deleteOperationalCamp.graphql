mutation DeleteOperationalCamp($campId: ID!) {
    deleteOperationalCamp(campId: $campId) {
        id
        name
        latitude
        longitude
        address
        campTypes
        personnelCount
        resourceAvailable
        status
        timestamp
    }
}